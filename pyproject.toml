[project]
name = "django-inertia-react-starter"
version = "0.1.0"
description = "Django + Inertia.js + React + shadcn-ui starter kit"
requires-python = ">=3.12"
dependencies = [
    "celery[redis]>=5.6.3",
    "django>=6.0.1",
    "django-celery-beat>=2.9.0",
    "inertia-django>=1.2.0",
]

